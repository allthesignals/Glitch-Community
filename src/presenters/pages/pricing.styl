.pricingBlocks
    display: grid
    grid-template-columns: repeat(auto-fill, minmax(var(--min-width, 300px), 1fr))
    grid-gap: u3
.pricingBlock
    background: primary-background
    border-radius: u05
    padding: u2